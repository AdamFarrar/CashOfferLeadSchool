{
  "name": "cash-offer-conversion-school",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "pnpm --filter @cocs/database drizzle-kit generate",
    "db:migrate": "pnpm --filter @cocs/database drizzle-kit migrate",
    "db:push": "pnpm --filter @cocs/database drizzle-kit push",
    "db:studio": "pnpm --filter @cocs/database drizzle-kit studio",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "@vitest/ui": "^4.0.18",
    "vitest": "^4.0.18"
  }
}